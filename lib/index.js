
/**
 * @param {String|Object} source Either a string of XML/SVG, or an object tree
 * representing the XML/SVG
 */
module.exports = function svgReactLoader (source) {
    var sourceType = typeof source;
    
    if (sourceType === 'string') {
        // is xml-like...
    }
    else {
        // is an object tree...
    }
}
